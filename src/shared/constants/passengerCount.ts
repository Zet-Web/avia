import { PassengerValue } from 'shared/types/passengerNumber'

export const passengerCount: number = 9

export const DEFAULT_PASSENGER: PassengerValue = {
  adults: 1,
  children: 0,
  infants: 0,
  isEconomSelected: true,
  isBusinessSelected: false,
}
